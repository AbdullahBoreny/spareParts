<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="spareParts.PageViews.Shop.OrdersPage"
             Title="Orders">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Frame Grid.Row="0" 
               BackgroundColor="LightBlue" 
               Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" 
                        Text="All" 
                        BackgroundColor="Blue" 
                        TextColor="White" 
                        CornerRadius="5" 
                        x:Name="AllOrdersButton" />
                
                <Button Grid.Column="1" 
                        Text="Pending" 
                        BackgroundColor="Transparent" 
                        TextColor="Blue" 
                        CornerRadius="5" 
                        x:Name="PendingOrdersButton" />
                
                <Button Grid.Column="2" 
                        Text="Completed" 
                        BackgroundColor="Transparent" 
                        TextColor="Blue" 
                        CornerRadius="5" 
                        x:Name="CompletedOrdersButton" />
            </Grid>
        </Frame>
        
        <CollectionView Grid.Row="1" 
                        x:Name="OrdersCollection" 
                        Margin="10">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10">
                        <Frame BackgroundColor="White" 
                               HasShadow="True" 
                               CornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <Label Grid.Row="0" 
                                       Text="{Binding OrderNumber}" 
                                       FontSize="16" 
                                       FontAttributes="Bold" />
                                
                                <Label Grid.Row="1" 
                                       Text="{Binding CustomerName}" 
                                       FontSize="14" 
                                       TextColor="Gray" />
                                
                                <Label Grid.Row="2" 
                                       Text="{Binding OrderDate, StringFormat='{0:MMM dd, yyyy HH:mm}'}" 
                                       FontSize="12" 
                                       TextColor="Gray" />
                                
                                <Label Grid.Row="0" 
                                       Grid.RowSpan="2" 
                                       Grid.Column="1" 
                                       Text="{Binding Status}" 
                                       FontSize="14" 
                                       FontAttributes="Bold" 
                                       TextColor="Green" 
                                       HorizontalOptions="End" />
                                
                                <Label Grid.Row="2" 
                                       Grid.Column="1" 
                                       Text="{Binding Total, StringFormat='${0:F2}'}" 
                                       FontSize="16" 
                                       FontAttributes="Bold" 
                                       TextColor="Blue" 
                                       HorizontalOptions="End" />
                            </Grid>
                        </Frame>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>
